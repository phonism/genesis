# Genesis Deep Learning Framework - Core Dependencies
# Install with: pip install -r requirements.txt

numpy>=1.20.0
torch>=2.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.23.0

# GPU computation (Linux/Windows only)
triton>=2.0.0; platform_system != "Darwin"
cuda-python>=12.0.0; platform_system != "Darwin"

tokenizers>=0.15.0
